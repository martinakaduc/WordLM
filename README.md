# Generic Language Model Word Level
## How to use
1. Put the corpus in the same place folder with this code
2. Run code
```
python main.py --corpus file_corpus.txt --epochs 100 --batch_size 128 --mode left2right

```
